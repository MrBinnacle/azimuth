# v1.2.0 — Pre-requisite Decision Gates

These 8 questions must be answered before the v1.2.0 bundle-vs-sequence
decision can be made cleanly. They were surfaced when AZIMUTH analyzed
its own v1.2.0 roadmap planning and returned INSUFFICIENT SIGNAL.

Resolve each gate and record the decision here before opening the v1.2.0
planning session.

---

## Gate 1 — Output-length ceiling (numeric)

**Why it blocks:** "Compounding bloat" is unfalsifiable without a number.
Can't evaluate length risk without a defined budget.

**Decision needed:** What is the maximum acceptable token/word count for
a STANDARD output? A DEEP output? Define numerically.

**Resolution:**

---

## Gate 2 — Template scope

**Why it blocks:** Default template only vs. all 6 templates is a ~5x
scope multiplier on any feature that touches output structure.

**Decision needed:** Do v1.2.0 features (evidence tags, audit trail) apply
to the default template only, or to all 6 templates?

**Resolution:**

---

## Gate 3 — Audit Trail: "always present" vs. "omit empty"

**Why it blocks:** ROADMAP.md describes Audit Trail as a transparency
feature, but current SKILL.md rule 2 specifies empty sections are omitted.
These conflict. Acceptance criterion AC3 cannot be written without
resolving this.

**Decision needed:** Does the Audit Trail section appear in every output
(even when all modules ran and found nothing notable), or only when there
is content to report?

**Resolution:**

---

## Gate 4 — Self-validation hard constraints in SKILL.md

**Why it blocks:** Without explicit hard constraints locked in SKILL.md
text, the self-validation pass will drift toward a second-verdict loop
over time — the skill second-guessing its own verdict in the same output.

**Decision needed:** What are the exact hard constraints on the
self-validation pass? Write them as SKILL.md-ready language before
implementation begins.

**Resolution:**

---

## Gate 5 — Output-length ceiling (enforcement mechanism)

**Why it blocks:** Gate 1 defines the budget. This gate defines what
happens when the budget would be exceeded — does the skill truncate,
compress, or flag to the user?

**Decision needed:** What is the enforcement behavior when output approaches
or exceeds the length ceiling?

**Resolution:**

---

## Gate 6 — Verdict-auditor pass: mandatory gate or advisory?

**Why it blocks:** Defines what "done" means for a v1.2.0 output. If
mandatory, it's a hard gate before the verdict is returned. If advisory,
it's a post-verdict annotation. These have different implementation shapes.

**Decision needed:** Is the verdict-auditor pass a required step in the
module sequence (verdict cannot be returned without it), or an optional
annotation layer?

**Resolution:**

---

## Gate 7 — Self-validation on INSUFFICIENT SIGNAL outputs

**Why it blocks:** v1.1.1 introduces the INSUFFICIENT SIGNAL verdict state.
v1.2.0 adds self-validation. The interaction is unresolved: does
self-validation run when the skill returns INSUFFICIENT SIGNAL, or does
INSUFFICIENT SIGNAL short-circuit the self-validation pass?

**Decision needed:** Does self-validation run on INSUFFICIENT SIGNAL
outputs? If yes, what does it validate when no verdict was produced?

**Resolution:**

---

## Gate 8 — Feature origin: user-requested vs. maintainer-driven

**Why it blocks:** Affects whether sequencing has a user cost. If evidence
tags and audit trail are user-requested (filed issues, inbound feedback),
sequencing delays affect real users. If maintainer-driven, the sequencing
cost is internal only and the discipline calculus changes.

**Decision needed:** By the time v1.2.0 planning reopens, classify each
v1.2.0 feature as user-requested (with evidence) or maintainer-driven.
This informs priority sequencing within the release.

**Resolution:**

---

## Unblock condition

All 8 gates resolved → reopen the v1.2.0 bundle-vs-sequence decision.

---

*Gate table produced by AZIMUTH analyzing its own v1.2.0 planning on
2026-05-05. The skill returned INSUFFICIENT SIGNAL on its own roadmap
question — the correct output given the undefined parameters.*
