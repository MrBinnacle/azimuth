# API keys — never commit
.env
.env.*
!.env.example

# Session state — never commit
.omc/

# Graphify knowledge graph — local only, rebuilt from source
graphify-out/

# Claude Code config — settings.json + hooks ship with the repo; everything else is local-only
.claude/*
!.claude/skills/
!.claude/settings.json
!.claude/hooks/
research/

# Internal strategy docs — not for public repo
MARKETING.md
CLAUDE.md
README_v1.1.1.md
docs/*
!docs/azimuth-runs
!docs/adr
!docs/agents
reddit-update-drafts.md

# Agent skill install artifacts — local only
.agents/
.continue/
.windsurf/
skills-lock.json
.claude/skills/azimuth/

# Playwright MCP cache
.playwright-mcp/

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Editor temp files
*.swp
*.swo
*~

# Editor configs
.vscode/
.idea/

# Local MCP config — contains tokens, never commit
.mcp.json
