# API keys — never commit
.env
.env.*
!.env.example

# Session state — never commit
.omc/

# Graphify knowledge graph — local only, rebuilt from source
graphify-out/

# Claude Code config — settings and session state are local-only
.claude/*
!.claude/skills/
research/

# Internal strategy docs — not for public repo
MARKETING.md
CLAUDE.md
README_v1.1.1.md
docs/*
!docs/azimuth-runs
!docs/adr
reddit-update-drafts.md

# Agent skill install artifacts — local only
.agents/
.continue/
.windsurf/
skills-lock.json

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Editor temp files
*.swp
*.swo
*~

# Editor configs
.vscode/
.idea/

# Local MCP config — contains tokens, never commit
.mcp.json
