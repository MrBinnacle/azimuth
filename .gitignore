# API keys — never commit
.env
.env.*
!.env.example

# Session state — never commit
.omc/

# Graphify knowledge graph output — local only, never tracked.
graphify-out/

# Claude Code config — settings.json + hooks ship with the repo; everything else is local-only
.claude/*
!.claude/skills/
!.claude/settings.json
!.claude/hooks/
!.claude/agents/
research/

# Maintainer-local docs — never public
CLAUDE.md
ROADMAP.md
MARKETING.md
README_v1.1.1.md
reddit-update-drafts.md

# docs/ is maintainer-local by default; specific public artifacts allow-listed.
docs/*
!docs/azimuth-runs
!docs/adr
!docs/agents
!docs/MAINTENANCE.md
!docs/VALIDATION.md
!docs/DESIGN.md
!docs/PRODUCT.md
!docs/STATE.md

# Agent skill install artifacts — local only
.agents/
.continue/
.windsurf/
skills-lock.json
.claude/skills/azimuth/

# Playwright MCP cache
.playwright-mcp/

# OS metadata
.DS_Store
Thumbs.db
desktop.ini

# Editor temp files
*.swp
*.swo
*~

# Editor configs
.vscode/
.idea/

# Local MCP config — contains tokens, never commit
.mcp.json
.azimuth-extraction-staging.md
